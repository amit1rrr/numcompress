language: python
python:
  - "2.6"
  - "2.7"
  - "3.3"
  - "3.4"
  - "3.5"
  - "3.5-dev"  # 3.5 development branch
  - "3.6"
  - "3.6-dev"  # 3.6 development branch
  - "3.7-dev"  # 3.7 development branch
  - "nightly"

install:
- pip install --upgrade coverage pytest unittest2 pytest-cov coveralls numpy

# command to run tests
script:
  - pytest --cov=numcompress

after_success:
  - coveralls  # test coverage